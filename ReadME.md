# Fund Data Chatbot

A Gradio-based chatbot that answers questions strictly from:
- holdings.csv
- trades.csv

### Rules
- No internet or external knowledge
- If data not found → "Sorry can not find the answer"

### Run locally
```bash
pip install -r requirements.txt
python app.py
